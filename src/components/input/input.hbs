<input 
    class="inner"
    autocomplete="off"
    type="{{type}}"
    name="{{name}}"
    minlength="{{minlength}}"
    maxlength="{{maxlength}}"
    pattern="{{pattern}}"
    placeholder=" "
    required
/>
