<input 
    id="{{id}}"
    class="inner"
    autocomplete="off"
    type="{{type}}"
    name="{{name}}"
    minlength="{{minlength}}"
    maxlength="{{maxlength}}"
    pattern="{{pattern}}"
    placeholder=" "
/>
